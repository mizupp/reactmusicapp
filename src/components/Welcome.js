import React, { useState } from 'react'

const Welcome = ({ name, genre, intro }) => {

    return (
     <>
     <h1> Music Site </h1>
     <p> this is the main page only</p>
     </>
    )
};

export default Welcome;
